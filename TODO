PRODUCT
- [ ] support multiple referendums in a branch
  - [ ] how to identify a referendum (if not by branch)? commit? tag? branch+name?
- [ ] generalize the poll/vote/tally flow to work for different types of referendums

FRAMEWORK
- [ ] automate temporary clone directory naming, simplify "git.LocalFromDir(files.WorkDir(ctx).Subdir("community"))"

DOC
- [ ] terminal-cast type video tutorials

FEATURES
- [ ] semantic human-readable stack traces
